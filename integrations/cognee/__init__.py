"""Cognee adapter (WeMakeDevs) — team code-review memory.

`get(mode)` in factory.py returns the offline JSON simulator (tests) or the
Cognee Cloud client (the demo backend). Follows the same sim/real adapter
contract as integrations/gitlab and integrations/dynatrace.
"""
