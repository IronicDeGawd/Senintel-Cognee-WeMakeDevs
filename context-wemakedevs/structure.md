# Structure — WeMakeDevs extension

> Tree of key dirs, entry points, and files touched for the WeMakeDevs work.
> Check this BEFORE Grep/Glob. Update whenever files are added/renamed/deleted.

## New for WeMakeDevs
```
context-wemakedevs/
  progress.md          # single source of truth (this hackathon)
  structure.md         # this file
  plan/                # feature specs — one file per feature
  research/            # SDK/API research (Cognee, WeMakeDevs partner tools)
```

## Existing base (reference — see context/structure.md for full tree)
```
dashboard/             # Next.js UI (landing + dashboard)
shared/                # mcp_client.py, Signal model, cross-pillar glue
tests/                 # pytest suite (pillar sims, correlation, api)
context/               # OLD hackathon planning (do not mix)
```

## Touched this hackathon
- (log files as they are created/modified)
