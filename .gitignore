# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Env / secrets
.env
.env.*
!.env.example
*-key.json
service-account*.json

# Node / Next.js
node_modules/
dashboard/.next/
dashboard/out/
dashboard/build/
npm-debug.log*

# Playwright MCP scratch shots (local verification only)
.playwright-mcp/

# Context tracked in this repo (shared research/plan for the team)

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Local data
data/
*.log
# Runtime artifacts (sim state). out/briefing.md is the tracked demo artifact.
# NOTE: git does not honor inline comments on a pattern line — keep them above.
out/incident_kb.json
out/signals.json
out/team_memory.json
out/eval_trends.json
# Per-run review notes emitted by scripts/run_code_review.py (throwaway).
out/mr_note_*.md
